import El from '@/Library'

export const ValidationText = ({ text, className }) => {
  return El({
    element: 'p',
    className: className,
    child: text,
  })
}
export const Validation = () => {
  
}
